version: '3.8'
services:
  db:
    image : postgres
    restart: always
    environment:
      - POSTGRES_USER=mhaddaou
      - POSTGRES_PASSWORD=mhaddaou
      - POSTGRES_DB=postgres
    ports:
      - '5432:5432'
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
